<!-- source-hash: 446e28496bf9439100817b3fbbac5649 -->
Integration sanity test for the `device_firmware` osquery table on Darwin (macOS), verifying the table can be queried without errors.

## Key Components

- **`deviceFirmware`** — Test fixture class inheriting from `testing::Test`, initializes the osquery test environment via `setUpEnvironment()`.
- **`test_sanity`** — Basic integration test that executes `SELECT * FROM device_firmware` and provides a scaffold for row count assertions and schema validation.

## Usage Example

```cpp
// Run the sanity test (typical osquery integration test invocation)
// The test queries the table and can be extended with validation:

ValidationMap row_map = {
    {"type",    NormalType},
    {"device",  NormalType},
    {"version", NormalType},
};
validate_rows(data, row_map);
```

## Notes

- Targets **Darwin only** — spec defined in `specs/darwin/device_firmware.table`.
- Row count assertions and schema validation are commented out, marking this as a minimal scaffold test.
- Extend by uncommenting `ASSERT_GE` / `ASSERT_EQ` calls and populating the `ValidationMap` to enforce expected column types and row counts.