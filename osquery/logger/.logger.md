<!-- source-hash: c7a38ca7991baf0b27a079a62854873b -->
Provides logging macros for the osquery framework, extending Google's `glog` library with table-specific and reference-annotated log helpers.

## Key Components

- **`TLOG`** — A verbose logging macro (`VLOG(1)`) intended for table plugin developers. Use this for non-critical parsing issues or expected edge cases rather than warnings/errors. Verbosity can be tuned via config or CLI flags.
- **`RLOG(n)`** — A string prefix macro that prepends a formatted reference number (e.g., `[Ref #42]`) to a log message, helping users cross-reference confusing log output with external documentation or issue trackers.

## Usage Example

```cpp
#include <osquery/logger/logger.h>

// Table verbose logging — only emitted when VLOG(1) is active
TLOG << "Parsed unexpected column value, skipping row";

// Reference-tagged log line for a known tricky condition
LOG(WARNING) << RLOG(1234) << "Filesystem mount point inaccessible";
// Output: [Ref #1234] Filesystem mount point inaccessible
```

> **Note:** `TLOG` output is suppressed by default. Enable it by setting the `--verbose` flag or adjusting the logging level in osquery config. Use `RLOG` alongside any standard `glog` severity macro (`LOG(INFO)`, `LOG(WARNING)`, etc.) — it is a plain string prefix, not a standalone logging call.