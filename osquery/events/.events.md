<!-- source-hash: 65c338a2ad9fa72549dcc9e9063aaf65 -->
Declares two utility functions for managing osquery's event publisher system, providing event loop initialization and query denylist enforcement for event-based tables.

## Key Components

| Symbol | Type | Description |
|--------|------|-------------|
| `attachEvents()` | `void` | Iterates the event publisher registry and spawns run loops for each publisher via the event factory |
| `enforceEventsDenylist()` | `bool` | Checks whether a query operates exclusively on event-based tables and should be denied execution |

## Usage Example

```c
#include "events.h"

// Initialize all registered event publishers at startup
osquery::attachEvents();

// Before executing a query, check if it should be blocked
std::string query = "SELECT * FROM process_events";
if (osquery::enforceEventsDenylist(query)) {
    // Query targets only event-based tables — skip or reject
}
```

> **Note:** Both functions operate within the `osquery` namespace. `attachEvents()` is typically called once during daemon initialization. `enforceEventsDenylist()` is used at query evaluation time to prevent abuse of high-frequency event-driven tables.